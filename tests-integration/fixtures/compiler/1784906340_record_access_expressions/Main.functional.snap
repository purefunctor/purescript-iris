---
source: tests-integration/src/fixtures.rs
---
@export identity = \value%0 -> value%0

@export record = { function: identity, nested: { value: 42 } }

@export direct = record.nested

@export chained = record.nested.value

@export functionPosition = record.function 1

@export argument = identity record.nested

@export applicationBase = (identity record).nested.value
